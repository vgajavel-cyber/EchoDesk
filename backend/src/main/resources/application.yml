spring:
  application:
    name: echodesk-api
  datasource:
    url: jdbc:postgresql://localhost:5432/echodesk
    username: echodesk
    password: echodesk
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
    locations: classpath:db/migration

server:
  port: 8080
